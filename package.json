{
  "name": "telegram-flight-app",
  "version": "1.0.0",
  "description": "Telegram Mini App for flight management",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step needed'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^1.0.0"
  }
}